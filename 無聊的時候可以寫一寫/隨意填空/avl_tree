/*
給定一棵二元搜尋樹（Binary Search Tree，BST），且該樹同時滿足 AVL 平衡。
每個節點的 size 欄位為「以該節點為根之子樹節點總數，包含自己」，所有鍵值互不相同。
typedef struct Node {
    int key;
    int size;
    struct Node *left;
    struct Node *right;
} Node;

int size(Node *node) {
    if (node == NULL) return 0;
    return node->size;
}
請完成以下 5 個空格，使 range_kth(root, low, high, k) 找出所有滿足 low ≤ key < high 的節點中第 k 小者；k 從 1 開始，若不存在則回傳 NULL。

int count_less(Node *node, int value) {
    if (node == NULL) return 0;
    if (node->key >= value)
        return count_less(node->left, value);
    return size(node->left) + 1 + (1)________________________;
}

Node* select_kth(Node *node, int k) {
    int left_size = size(node->left);
    if (k == (2)__________________) return node;
    if (k <= left_size)
        return select_kth(node->left, k);
    return select_kth(node->right, (3)_______________________);
}

Node* range_kth(Node *root, int low, int high, int k) {
    int before = count_less(root, low);
    int target_rank = (4)____________________________________;
    if (k <= 0 || (5)_______________________________________)
        return NULL;
    return select_kth(root, target_rank);
}
填入 5 個空格。（10 分）
1. count_less(node->right , value)
2. left_size + 1
3. k - left_size - 1
4. before + k
5. target_rank > count_less(root, high)



若樹的鍵值為 10、20、30、40、50、60、70，求 range_kth(root, 25, 65, 3) 的結果並列示秩的計算。（4 分）
分析在 AVL 平衡成立時的時間複雜度。（4 分）

*/